;; Files that will be checked for blog posts
;; (setq blog-files (list "posts/blog.org" "posts/blog-extra.org"))

;; Root directory of blog
;; (setq blog-root "/path/to/blog")

;; Directory where the posts will be exported
;; (setq blog-export-directory "posts-html")

;; Path to the blog's database
(setq database-path "blog.db")

;; Path to the authors file
(setq authors-file-path "authors.json")

;; Number of avatars in an avatar set
(setq avatar-set-size 5)

;; If blog.el is installed in a directory that's not in your load path, you can modify it here
;; (add-to-list 'load-path "/path/to/blog.el/directory")
